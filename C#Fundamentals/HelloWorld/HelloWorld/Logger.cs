using System;

namespace HelloWorld
{
    public class Logger
    {
        // we wil add open file and write to the file and close the file finaly save the file to desk
        public void Log(string message)
        {
            Console.WriteLine(message);
        }
    }
}